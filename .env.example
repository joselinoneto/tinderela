# UEX API 2.0 bearer token — create one at https://uexcorp.space/api/apps
UEX_API_TOKEN=

# Optional: path to the SQLite cache database (default: ./cache.db)
# SC_TRADE_DB=./cache.db

# --- Discord bot (npm run bot) ---
# Anthropic API key for the answer agent — https://console.anthropic.com
ANTHROPIC_API_KEY=
# Discord bot token — https://discord.com/developers/applications
DISCORD_BOT_TOKEN=
